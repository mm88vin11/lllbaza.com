# Что сделано с сайтом и что осталось

Работа шла по одному правилу: не советовать, а править. Ниже — что было не
так, что стало, чем это проверено и что осталось за вами.

---

## 1. Аудит: что нашли

Приоритеты: **P0** — ломает первый экран, индексацию, безопасность или право.
**P1** — заметно бьёт по доверию и заявкам. **P2** — качество.

| # | Проблема | Где | Чем это оборачивалось | Приоритет | Статус |
|---|---|---|---|---|---|
| 1 | Токен Telegram-бота лежал в исходнике страницы, зашифрованный XOR-ом | `TGX`/`TGK`, `tgSecret()` | От сканеров защищало, от человека с консолью — нет. Любой мог писать от имени бота | **P0** | Исправлено |
| 2 | Кадр заставки вписывался целиком и ещё ужимался на 7 % | `cover()`, `holdZ` | Светлый прямоугольник кадра на чёрном поле, видимая граница сверху и снизу. Ровно то, на что жаловались | **P0** | Исправлено |
| 3 | Страница ничего не рисовала, пока не смонтируется рантайм | вся страница | Lighthouse: NO_FCP, производительность не считалась. Робот видел пустую страницу | **P0** | Исправлено |
| 4 | Кадры заставки лежали в base64 внутри документа | 20,7 МБ `index.html` | Браузер обязан выкачать 20 МБ до первого экрана. На телефоне — десятки секунд | **P0** | Исправлено |
| 5 | Карточки ленты пустые: `image-slot` не заполнен, видео нет | 7 карточек | «ТикТок» без единого кадра — главный блок доказательств пустой | **P0** | Исправлено |
| 6 | Не было политики cookie и пользовательского соглашения | правовой блок | Баннер про cookie есть, документа о них нет | **P0** | Исправлено |
| 7 | Цены в разметке JSON-LD расходились с ценами на странице | `seo:start` | 250 000 / 900 000 / 2 500 000 против 1 000 $ / 350 000–1 200 000 / 1 200 000 | **P0** | Исправлено |
| 8 | Не было блока вопросов | — | Цена, сроки и «а если не сработает» не отвечены нигде | **P1** | Исправлено |
| 9 | Лента на телефоне двигалась кадр в кадр за прокруткой | `fDrive()` | Каждый рывок инерции доезжал до карточки один в один — дёргалось | **P1** | Исправлено |
| 10 | Цикл анимации крутился 60 раз в секунду всегда | `step()` | Грел телефон на дочитанной странице, не давал главному потоку пауз | **P1** | Исправлено |
| 11 | Белёсый текст давал 2,5–3,4:1 при норме 4,5:1 | ~80 мест | Мелкие подписи нечитаемы, WCAG AA не выполняется | **P1** | Исправлено |
| 12 | 19 элементов гасили кольцо фокуса через `outline: none` | по всему коду | С клавиатуры сайт проходится, но непонятно, где ты | **P1** | Исправлено |
| 13 | Доступное имя кнопок ленты не содержало видимую подпись | `fRail*` | WCAG 2.5.3: «нажми оценить» голосом не срабатывало | **P1** | Исправлено |
| 14 | Засечки прогресса — 15×14 px | `fSegs` | Меньше минимума 24×24, попасть пальцем трудно | **P1** | Исправлено |
| 15 | Не было отдельной страницы «спасибо», 404 и 500 | — | Нет цели в аналитике, 404 отдаёт страницу хостинга | **P1** | Исправлено |
| 16 | Не было `robots.txt`, `sitemap.xml`, `llms.txt`, манифеста, полного набора иконок | — | Иконка одна — инлайновый SVG; карты сайта нет | **P1** | Исправлено |
| 17 | Не было заголовков безопасности и редиректов на канонический адрес | — | Ни CSP, ни HSTS; www и non-www живут порознь | **P1** | Исправлено |
| 18 | Время ответа обещалось тремя разными сроками | 3 места | «рабочий день» и «час» на одной странице | **P2** | Исправлено |
| 19 | Карусель кейсов удалена из вёрстки, код остался | `WK`, `works`, `kaseTick` | ~6,8 КБ мёртвого кода, считался каждый кадр | **P2** | Исправлено |
| 20 | `og:image` вёл на несуществующий файл | `seo:start` | Ссылка в мессенджере — без картинки | **P2** | Исправлено |

---

## 2. Что именно изменилось

### Первый экран: поля и стыки

Кадр вписывался целиком (`Math.min`) и умножался на 0,93 — то есть заведомо
рисовался меньше экрана. Края у кадров не чёрные: замер по `seqd/f048` даёт
около `#312c32` против фона `#0a0a0a`. Растушёвка такой край не прятала —
прямоугольник читался всё равно.

Теперь кадр **заливает** экран. Обрезка ограничена (30 % на телефоне, 22 % на
ПК), чтобы ноутбук не срезало на нестандартных пропорциях, а остаток поля
закрывается **продолжением самого кадра**: крайний столбец растягивается
вбок, крайняя строка — вверх и вниз. Цвет на стыке совпадает попиксельно,
поэтому границы нет физически, ни при каком соотношении сторон.

Проверено на 1440×900, 2560×1080 (21:9), 820×1180 (планшет в портрете) и
390×844. Замер перепадов яркости по строкам: резких переходов на границах
кадра не осталось — все найденные скачки приходятся на содержимое самого
кадра (крышка ноутбука, край стола).

### Лента: на весь экран и без рывков

Семь карточек получили настоящие вертикальные кадры 9:16, снятые под
фирменный стиль: чёрная студия, тёплый бронзовый контровый свет, фигура с
белой плашкой вместо головы. Пока карточка на экране, кадр очень медленно
наезжает — движение почти не читается глазом, но лента перестаёт выглядеть
галереей скриншотов. Вес всех семи — 212 КБ.

Позиция ленты больше не присваивается из прокрутки кадр в кадр. Прокрутка
задаёт **цель**, лента идёт к ней с затуханием (постоянная 55 мс) — рывки
инерции до карточки не доходят, а палец по-прежнему ведёт ленту без
задержки. Перелистывание идёт по smootherstep: у него нулевые скорость и
ускорение на концах, поэтому ни начала, ни конца перелиста не видно как
толчка. Удержание сокращено с 62 % до 34 % хода — карточка меньше «залипает».
Геометрия секции читается один раз на resize, а не каждый кадр.

На телефоне в полном погружении карточка занимает весь экран: скругления
уходят в ноль, шапка, нижняя строка и полоса прогресса гаснут.

### Скорость

Три вещи по очереди:

1. **Оффер в разметке.** Заголовок, подзаголовок, список направлений и кнопка
   лежат прямо в HTML и рисуются первым кадром, без единой строчки скрипта.
   Первая отрисовка содержимого: **736 мс → 120 мс**. Уходит этот экран не по
   монтированию, а когда интро реально начало играть, — иначе между оффером и
   логотипом мигал чёрный кадр. Он же остаётся всей страницей, если скрипты
   отключены: прежняя заглушка удалена за ненадобностью.
2. **Медиа отдельными файлами.** 266 кадров заставки + логотипы весили 20 МБ в
   base64 внутри документа. Теперь это обычные файлы: грузятся параллельно, в
   том порядке, который задаёт компонент, кешируются по отдельности.
   `index.html`: **20,7 МБ → 1,10 МБ** (327 КБ под gzip). Логотипы интро и
   подвала дополнительно дублировались в документе по два раза — 800 КБ на
   четыре картинки, из которых уникальных две.
3. **Цикл засыпает.** Полный проход теперь идёт только когда что-то
   действительно едет: прокрутка, инерция, переход по якорю, обновление
   состояния, открытое окно, стенд сборки, недокачанные кадры. В покое — раз
   в шесть кадров вместо каждого.

Режим одного файла остался: `node build/build.mjs --inline` кладёт всё обратно
внутрь и даёт 20 МБ, который открывается с диска без сервера.

### Заявки

Токен убран из клиента целиком. Доставку ведёт `server/worker.js`
(Cloudflare Worker): проверяет источник, ограничивает частоту (5 заявок с
адреса за 10 минут через KV), режет тело больше 24 КБ, чистит и обрезает
каждое поле перед вставкой в сообщение, наружу отдаёт общую ошибку, подробности
пишет в лог.

Пока адрес обработчика не задан, форма работает честно: заявка ложится в
`localStorage` **до** отправки, экран говорит «сохранена, но канал не
отвечает», предлагает продублировать одним нажатием (текст уже в буфере) и
сама дошлёт её при следующем заходе. Проверено настоящей отправкой:
`sent: true`, `sendOk: -1`, очередь 1, адрес сменился на `/spasibo`.

### Тексты

Блок из пяти вопросов — тех, после которых человек либо пишет, либо закрывает
вкладку. Неудобные стоят первыми: сколько стоит, а если не сработает, сколько
ждать, что нужно от меня, что будет после сдачи. Каждый ответ — из его
ситуации, а не про нас. Разбор за 25 000 ₽ назван тем, чем он и является:
способом не платить за догадки, с деньгами в счёт проекта.

Время ответа обещалось в трёх местах по-разному — «в течение рабочего дня» в
конфигураторе и «в течение часа» в двух новых. Час взялся из воздуха, свели
всё к тому, что на сайте уже было.

### Доступность

- Нижняя граница белёсого текста поднята до 0.52 альфы (4,9:1 на фоне
  секции), тёмного на кремовом — до 0.6 (5:1). Правка прошла по всем
  литералам, включая тернарники и helper `cream()`. Иерархия сохранена: то,
  что было приглушено, приглушено и осталось.
- Кольцо фокуса возвращено на `:focus-visible` — то есть только для
  клавиатуры, вид при работе мышью не изменился. На кремовых плашках кольцо
  тёмное.
- Кнопкам ленты вернули видимую подпись в доступное имя.
- Засечкам прогресса — 26×26 на нажатие.
- `prefers-reduced-motion` работал и работает: сцена сжимается до одного
  экрана, бегущие строки и пульсации стоят.

### Право

Было два документа, стало четыре: политика конфиденциальности, согласие на
обработку, **политика в отношении cookie** и **пользовательское соглашение**.
Все четыре — вкладками в одном окне, ссылки в подвале.

---

## 3. Файлы

**Новые**

```
README.md                     как собрать и опубликовать
ОТЧЁТ.md                      этот файл
src/page.html                 исходник страницы (правим только его)
src/assets/media/**           280 файлов: кадры, картинки ленты, логотипы
src/assets/runtime.json       копии React
build/build.mjs               сборка index.html, два режима
build/pages.mjs               404, 500, «спасибо»
build/dist.mjs                сборка папки для хостинга
build/set-lead.mjs            подстановка адреса обработчика заявок
build/code.sh, build/js.sh    просмотр кода без base64
server/worker.js              приём заявок
server/wrangler.toml          его настройки
public/404.html 500.html spasibo.html
public/favicon.ico .svg apple-touch-icon.png icon-192 icon-512 icon-maskable-512
public/og.jpg                 картинка для ссылок, 1200×630
public/robots.txt sitemap.xml llms.txt site.webmanifest
public/_headers _redirects    заголовки безопасности и канонический адрес
netlify.toml                  сборка и публикация
.gitignore .env.example
.claude/skills/**             подключённые наборы правил по дизайну
```

**Изменённые:** `index.html` — теперь результат сборки, а не исходник.

---

## 4. Проверки

Замер на локальном сервере с gzip, как у хостинга.

| | ПК | Телефон |
|---|---|---|
| Производительность | **100** | **91** |
| Доступность | **100** | **100** |
| Лучшие практики | **100** | **100** |
| SEO | **100** | **100** |
| Первая отрисовка содержимого | 0,5 с | 2,5 с |
| Крупнейшая отрисовка | 0,5 с | 2,6 с |
| Сдвиг вёрстки (CLS) | 0 | 0 |
| Блокировка потока (TBT) | 20 мс | 160–280 мс |

Было: производительность не считалась вовсе (NO_FCP), доступность 96.

Остальное:

- **Сборка** — проходит в обоих режимах, разметка компонента парсится без ошибок.
- **Консоль** — чисто на ПК и на телефоне. Единственная запись,
  `ERR_CERT_AUTHORITY_INVALID`, — свойство песочницы, где шёл замер: запрос
  курса ЦБ не проходит проверку сертификата через прокси. На обычном хостинге
  этого не будет.
- **Форма** — проверена настоящей отправкой (см. выше).
- **Клавиатура** — 14 переходов по Tab, ссылка «к содержанию» работает,
  кольцо фокуса видно на каждом элементе.
- **Заставка** — снята на 1440×900, 2560×1080, 820×1180, 390×844: полей и
  границ нет ни на одном.
- **Правовое окно** — четыре вкладки, `aria-current` переключается.
- **404 и «спасибо»** — открываются, редирект `/spasibo` работает.
- **Разметка** — `sitemap.xml` и `site.webmanifest` валидны, JSON-LD
  разбирается, `robots.txt` ссылается на карту сайта.
- **Секретов в коде нет**: `TGX`, `TGK`, `tgToken`, `tgChat` — ноль вхождений.

Чем **не** проверено, честно: Rich Results Test, реальные заголовки от
хостинга и Lighthouse на живом домене — для всего этого нужен опубликованный
сайт. Список того, что там смотреть, — ниже.

---

## Второй заход: правки по разметке владельца

Разбор шёл по одиннадцати скриншотам со стрелками и подписями. Ниже — что
именно поменялось.

### Первый экран и фон

| Было | Стало |
|---|---|
| При загрузке мелькал текстовый экран с оффером, кнопками и подвалом | Со скриптами с первого кадра видно нашу заставку: тот же чёрный, та же полоса. Текстовый экран остался для поиска и для тех, у кого скрипты выключены |
| Фон — две картинки 160×90 px, растянутые на 136 % экрана | Фон собран градиентами: тянется на любой размер без единой ступеньки. Именно растяг вдвенадцатеро и давал «кубики» |
| Курсор на заставку не влиял | Кадр уходит от курсора с наклоном в глубину и возвращается, когда рука ушла — как заставка в GTA |
| Шум-дизер 5 % на 190 px | 8,5 % на 128 px в режиме наложения: разбивает полосы в тёмных градиентах |

### Движение

| Было | Стало |
|---|---|
| Нижняя строка показателей шла 34 px/с и считалась раз в шесть кадров на неподвижной странице | 48 px/с и отдельный такт вне экономного режима — идёт ровно всегда |
| Фонарь под курсором в подвале тормозил по той же причине | Пока фонарь горит, цикл не засыпает |
| Появление блоков начиналось, когда блок уже въехал в экран на 14 % | Начинается за 12–14 % до входа; ход сокращён с 1080 до 760 мс |
| Заливка кнопки — полоса слева направо | Свечение под курсором, как фонарь на логотипе в подвале: разгорается за 620 мс, гаснет за 440, идёт следом за рукой. Цвет берётся по замеренной яркости кнопки — на кремовой тёмный, на тёмной кремовый. Перекат подписи по буквам убран |

### Блоки

| Было | Стало |
|---|---|
| «Отметьте то, что узнаёте» — счётчик «2 из четырёх» | Счётчик часов и рублей: сколько уходит в месяц и во что это выливается за год. Допущения расчёта написаны рядом |
| «Кто это делает» — четыре вкладки, под каждой список плюсов и минусов | Четыре колонки рядом: рисунок, три шкалы и одна строка. Сравнение делает глаз |
| Лента услуг — телефон 324 px, колесо крутило страницу мимо | Телефон во весь доступный рост; колесо над лентой перелистывает карточку целиком и отдаёт жест странице на краю |
| Цены — шестнадцать строк с галочками в две колонки | Фишки, которые наливаются по ходу ползунка. На телефоне видно только то, что входит, остальное свёрнуто в счётчик |
| «Собрать проект» — кремовый текст на кремовой кнопке | Миниатюра верстака, короткая строка и стрелка |
| «Вам — разбор, а не проект» — абзац и три кнопки в столбик | Вывод, две причины и один шаг |
| Карта маршрута — три абзаца вдоль вертикальной черты | Дорога, которая прочерчивается по прокрутке, и три станции на ней |
| «Не возьмёмся» и «что дальше» — семь длинных строк | Три перечёркнутых знака и лестница из четырёх ступеней |
| Блок пяти вопросов | Удалён вместе с разметкой `FAQPage` |
| Конфигуратор — модули по орбите вокруг ядра | Верстак: сетка три на три, стрелка, ячейка результата. Плюс две шкалы — вилка и срок. Размер команды убран: заказчику важно, что и когда он получит |
| Карта городов — 19 точек, среди них Бишкек и Ташкент | 32 города: больше России, немного Европы, Бангкок и Пхукет. Дуги и точки пересчитаны по проекции карты |
| Подвал — ссылки колодцем вдоль левого края, клеймо на пол-экрана | Три колонки, строка копирайта, клеймо узкой полосой |

### Право

Документы переписаны под требования, которые предъявляют к российскому сайту
с приёмом заявок:

- политика обработки публикуется во исполнение ч. 2 ст. 18.1 152-ФЗ;
- названы правовые основания по ст. 6, способы обработки, сроки хранения;
- отдельно сказано, что базы данных находятся в России (ч. 5 ст. 18 152-ФЗ) и
  что трансграничной передачи нет;
- описаны меры защиты по ст. 18.1 и 19, срок ответа на запрос — десять рабочих
  дней по ст. 20;
- согласие на рекламу вынесено во **вторую отдельную галочку**, не отмеченную
  заранее: ст. 18 38-ФЗ требует, чтобы оно было предварительным и отдельным;
- обе отметки уходят вместе с заявкой — оператор должен уметь доказать, что
  согласие было получено;
- обе галочки работают с клавиатуры (`role="checkbox"`, пробел, Enter).

### Третий заход: графика, экран конфигуратора, текст

| Было | Стало |
|---|---|
| Карточки итогов под картой маршрута — четыре линейные иконки | Четыре предметные сцены, сгенерированные через коннектор: песочные часы, растущие цилиндры, пустой стул, шар впереди ряда. Один свет со всем сайтом — чёрный фон, кремовый объект. 680 px, WebP, 6–11 КБ каждая |
| Конфигуратор — 1274 px при окне 900 на ПК и 1351 при 844 на телефоне | 899 и 838: помещается в экран целиком. Плитки встали в два ряда по четыре, список под верстаком прокручивается сам, как инвентарь в игре, страница под пальцем стоит |
| Нижняя кнопка при пустой сборке гасла до 55 % и ловила курсор «нельзя» | Обводка и прямая подсказка «Отметьте направление». Сплошной кремовой становится, когда есть что считать |
| Пустое гнездо верстака было темнее корпуса и сливалось с ним | Светлее фона, тень сверху-слева, блик снизу-справа — утоплено, как в настоящем верстаке. Занятое приподнято |
| Шестнадцать фишек вилки висели на большом экране разом, двенадцать из них притушены до нечитаемости | Видно то, что входит, плюс счётчик остального. Ползунок список наращивает, а не перекрашивает |
| Строка под шагом маршрута слово в слово повторяла карточку «на руках у вас» | Осталось что-то одно |
| Затемнение в ленте: сверху 60 % черноты, снизу стена на треть высоты | 40 % сверху, чистая середина — от съёмки остаётся съёмка, а не силуэт |
| Заставка на телефоне — 340 svh, «Вы не владелец» начиналось на 2.82 экрана | 190 svh: заголовок встречает на 1.32 экрана. Кадры те же, все 132 |

Текста стало меньше там, где его читают глазами, а не по диагонали:

| Блок | Было слов | Стало |
|---|---|---|
| Цены | 290 | 161 |
| Конфигуратор | 184 | 126 |
| Карта маршрута | 192 | 157 |

В ленте услуг 345 слов, и это нормально: они разложены по семи карточкам,
на экране одновременно около двадцати пяти.

---

---

## 5. Что осталось за вами

### Без этого нельзя публиковать

1. **Домен и HTTPS.** `_redirects` и `netlify.toml` уже сводят www, non-www и
   http к `https://lllbaza.ru`. Нужен сам домен и сертификат.
2. **Обработчик заявок.** Пока адрес не задан, заявки только копятся в
   браузере посетителя:
   ```sh
   cd server && npx wrangler deploy
   npx wrangler secret put TG_TOKEN
   npx wrangler secret put TG_CHAT
   cd .. && node build/set-lead.mjs https://<ваш-адрес>/ && node build/build.mjs
   ```
3. **Отозвать старый токен.** Он лежал в опубликованном исходнике — считайте
   его скомпрометированным. У @BotFather: `/revoke`, потом завести **отдельного**
   бота, который умеет только писать в один чат.
4. **Ограничение частоты.** В `server/wrangler.toml` заведите KV namespace и
   раскомментируйте `[[kv_namespaces]]`. Без него воркер работает, но без
   ограничения.
5. **Лимит расходов** в биллинге Cloudflare и алерт на превышение.

### Нужны ваши данные

0. **Новый логотип — файлом.** Это единственная правка из вашего списка,
   которую я не сделал, и причина простая: логотип пришёл картинкой в чате.
   Оттуда его нельзя достать файлом — я вижу изображение, но не могу
   сохранить его в репозиторий. Перерисовывать ваш фирменный знак «на глаз»
   я не стал: логотип должен совпадать с оригиналом до кривой, а не
   напоминать его.

   Пришлите его как файл — **SVG** лучше всего (масштабируется без потерь,
   весит копейки), подойдут и **PNG** с прозрачным фоном от 1024 px, лучше
   две версии: светлая и тёмная. Я поставлю его в шапку, на заставку, в
   подвал и в иконку вкладки, пересоберу и запушу — это работа на один
   заход.

   Пока в шапке, на заставке и в подвале стоит прежний знак
   (`logo-lockup.webp`, `logo-eto-2x.webp`, `logo-baza-2x.webp`).


6. **Счётчик аналитики.** `ANALYTICS = { ym: 0, ga: '' }` в `src/page.html` —
   поставьте номер Яндекс.Метрики, и цели поедут сами: `quiz_started`,
   `form_step1`, `form_sent`, `form_delivered`, `faq_open`, `thanks_view`,
   `tg_click`. Счётчик поднимается **только** после согласия на cookie.
   Цель на заявку — просмотр `/spasibo`.
7. **Отзывы.** На сайте есть отзывы с именами и ролями (Юлия, основательница
   бренда одежды; и другие). Я их не трогал и не могу подтвердить. Проверьте
   каждый: если человек реально это говорил — попросите разрешение и, если
   можно, ссылку на профиль. Если нет — убрать. Фейковый отзыв стоит дороже,
   чем отсутствие отзывов.
8. **Цифры в кейсах.** «Месяц после запуска = три предыдущих», «повторных
   заказов вдвое больше» — то же самое: либо подтверждаются, либо уходят.
9. **Фото команды.** Его нет. Живое фото в разделе «кто это делает» закрывает
   половину вопросов о доверии.
10. **Реквизиты — единственное, что я не мог заполнить сам.** В коде есть
    объект `LEGAL` (ищите по слову `LEGAL = {`), и в нём четыре пустых поля:
    `inn`, `ogrnip`, `addr`, `email`. Впишите свои — и они сами появятся
    в подвале, во всех четырёх документах и в форме согласия. Пустые поля
    подвал не рисует, а документы вместо них честно пишут «полные реквизиты
    предоставляем по запросу».

    Выдумать их за вас я не мог: ИНН и ОГРНИП — государственные номера, и
    неверный номер на сайте хуже, чем его отсутствие. Для российского сайта,
    который принимает заявки, они обязательны — без них оператор персональных
    данных формально не идентифицирован.
11. **Юрист.** Четыре документа написаны по существу и без выдуманных ссылок
    на статьи, но это не юридическая экспертиза. Покажите их юристу — особенно
    в части обработки персональных данных и роли Telegram как получателя.
12. **Уведомление РКН.** Если собираете персональные данные как ИП —
    уточните у юриста, нужно ли подавать уведомление об обработке.

### Можно сделать позже

13. **CSP без `unsafe-inline`.** Сейчас он нужен: страница — один файл с
    инлайновым рантаймом. Убрать его можно только вместе с переездом на сборку
    с внешними скриптами и хешами.
14. **Кадры в AVIF** рядом с WebP через `<picture>` — минус ещё 25–30 % к весу
    заставки.
15. **Своё видео в ленте.** Карточки уже умеют показывать `<video>` — поле
    `video` в `FEEDD` пустое. Настоящие 5–10-секундные ролики вместо кадров
    поднимут доверие сильнее любой картинки.
16. **Второй язык**, если целитесь в Дубай и США: сейчас страница только
    русская (`lang="ru"`).

---

## 6. Чего я намеренно не делал

- **Не трогал тексты, которые уже работают.** Свод «Законов продаж» на сайте
  соблюдён: продаётся результат, а не часы; боль конкретная и своя; цена —
  вилкой с одной рекомендацией; кнопки про следующий шаг, а не «хотите
  купить». Переписывать было нечего — только дописать недостающее.
- **Не выдумывал отзывы, цифры, кейсы и номера законов.**
- **Не добавлял вторую липкую кнопку на телефоне.** Шапка уже держит
  постоянный CTA «Где теряю»; второй конкурирующий призыв сделал бы хуже.
- **Не минифицировал код страницы.** Экономия оказалась 4 % — всё место
  занимают строки и объекты стилей, а не пробелы. Риск сломать рантайм того
  не стоит.
- **Не удалял фирменное интро.** Оно держит экран лишние секунды, но это
  осознанное решение бренда, а не дефект. Статический оффер под ним теперь
  закрывает и робота, и того, у кого медленный телефон.
