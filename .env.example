# Значений здесь нет и быть не должно — это список того, что нужно задать,
# а не файл с секретами. Токены живут в секретах воркера:
#   npx wrangler secret put TG_TOKEN
#   npx wrangler secret put TG_CHAT

# Токен бота от @BotFather. Заведите отдельного бота, который умеет только
# писать в один чат: если его всё-таки достанут, терять будет нечего.
TG_TOKEN=

# Куда бот пишет заявки: id чата или канала.
TG_CHAT=
